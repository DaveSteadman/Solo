# KoreUI Page Script Demo

This repo demonstrates a constrained page-script architecture:

- JSON page intent
- schema validation
- deterministic renderer
- reusable primitive controls
- first-class domain blocks

To exercise the JSON path in a browser, run a simple static server from the repo root, for example:

